#! /bin/bash

agda --html --html-highlight=auto --html-dir=docs Index.lagda.md

for i in `find docs -name "*.md"`; do
  title=$(echo $i | awk 'BEGIN{FS="[.,/]"}{print $2}')
  pandoc --standalone $i --template template.html -o docs/$title.html
  rm $i
done
